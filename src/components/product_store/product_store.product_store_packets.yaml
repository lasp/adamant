---
description: Packets for the Product Store component. The contents of this packet are populated based on the stored products model provided to the Product Store component at instantiation.
packets:
  - name: Stored_Products
    description: This packet contains the contents of the data product store managed by this component, including the store CRC, the save time (if configured), and each stored data product (with its timestamp, if configured).
