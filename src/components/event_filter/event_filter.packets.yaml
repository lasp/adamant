---
description: Packet to dump the event filter state of all events. Each event state takes one bit.
packets:
  - name: Event_Filter_State_Packet
    description: The packet used to dump all the state information for which events are filtered and which are not. Each event ID takes a bit and any extra bits beyond the event range will show as not filtered.

