---
description: A packed record which holds a packet identifier and period.
with:
  - Packet_Types
fields:
  - name: Id
    description: The packet identifier
    type: Packet_Types.Packet_Id
    format: U16
  - name: Period
    description: The packet period, in ticks
    type: Natural
    format: U32
