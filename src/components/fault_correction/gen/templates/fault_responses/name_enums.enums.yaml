---
description: These are autogenerated enumeration types for the {{ name }} assembly.
enums:
  - name: Fault_Type
    description: This enumerations lists all the possible fault conditions possible in the assembly. The value of the enumeration corresponds to the fault identifier for that fault.
    literals:
      - name: None
        value: 0
        description: No fault present.
{% for response in responses %}
      - name: {{ response.full_fault_name_str }}
        value: {{ response.fault_id }}
{% if response.description %}
        description: "{{ response.description }}"
{% endif %}
{% endfor %}
