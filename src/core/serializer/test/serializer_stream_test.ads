-- The Stream_Serializer file round-trip can only run where a
-- filesystem exists. Target-selected body: the Linux body performs
-- the round-trip, the bareboard body is a no-op.
package Serializer_Stream_Test is

   procedure Run;

end Serializer_Stream_Test;
