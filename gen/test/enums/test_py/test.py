#!/usr/bin/env python3

# Build our dependencies using the build system.
# This is necessary because some of the dependencies we
# have are autogenerated.
from util import pydep

pydep.build_py_deps()

from test_enums import First_Enum, Second_Enum
import sys


def println(strn=""):
    sys.stderr.write(strn + "\n")


# Main:
if __name__ == "__main__":
    println("create enums:")
    fe = First_Enum(1)
    println(str(fe))
    println(str(fe.to_tuple_string()))
    println(str(fe.to_string()))
    try:
        fe = First_Enum(-1)
        assert False, "should not get here."
    except ValueError:
        pass
    se = Second_Enum(10)
    println()
    println(str(se))
    println(str(se.to_tuple_string()))
    println(str(se.to_string()))
    try:
        se = Second_Enum(12)
        assert False, "should not get here."
    except ValueError:
        pass
    println("passed.")
    println()
