%- if description
\VAR{description|e}
\vspace{5mm}
%- endif

%- if preamble 
\vspace{10mm}
\textit{Preamble (inline Ada definitions):}\\
\texttt{\VAR{preamble|e}}
%- endif
\vspace{5mm}
%- for enum in enums.values()
\textbf{\texttt{\VAR{enum.name|e}}}:
%- if enum.description
\VAR{enum.description|e}
%- endif
\captionof{table}{\VAR{enum.name|e} Literals: }
\begin{xltabular}{\textwidth}{ | l | c | X |}
  \hline
  \textbf{Name} & \textbf{Value} & \textbf{Description} \\ \hline
%- for literal in enum.literals
  \texttt{\url{\VAR{literal.name}}} &
  \texttt{\url{\VAR{literal.value}}} &
%- if literal.description
  \texttt{\VAR{literal.description|e}} 
%- endif
  \\ \hline
%- endfor
\end{xltabular}
\vspace{5mm} %5mm vertical space
%- endfor
