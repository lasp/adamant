%- if parameters 
%- if parameters.description
\VAR{parameters.description} \\
%- else
\par Below is a list of the parameters for the \VAR{prettyname|e} component.
%- endif

\captionof{table}{\VAR{prettyname|e} Parameters}
\begin{xltabular}{\textwidth}{ | c | l | X | X | }
  \hline
  \textbf{Local ID} & \textbf{Parameter Name} & \textbf{Type} & \textbf{Default Value} \\ \hline
%- for param in parameters 
  \texttt{0x\VAR{"%04x" % loop.index0 } (\VAR{ loop.index0 })} &
  \texttt{\url{\VAR{param.name}}} &
  \texttt{\url{\VAR{param.type}}} &
%- if param.default_value
  \texttt{\url{\VAR{param.default_value}}}
%- else
  \textit{None}
%- endif
  \\ \hline
%- endfor
\end{xltabular}
\vspace{5mm} %5mm vertical space

Parameter Descriptions:
\begin{spaceditemize}
%- for param in parameters 
  \item \textbf{\texttt{\VAR{param.name|e}}} - 
%- if param.description
    \VAR{param.description|e}
%- else
    \textit{No description provided.}
%- endif
%- endfor
\end{spaceditemize}
\vspace{5mm} %5mm vertical space
%- else
\par The \VAR{prettyname|e} component has no parameters.
%- endif
